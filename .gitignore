# Examples
*.spec.js

Wait, that's wrong - we shouldn't ignore test files. Since the only change is a test file modification and no build artifacts or dependencies are present, output nothing.

(tests/example.spec.js is a legitimate test file that should not be ignored)